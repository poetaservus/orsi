"""Tool contracts and built-in capability implementations.

Import contracts, registry types, and concrete tools from their defining modules. Security and
execution infrastructure deliberately live in separate packages.
"""
